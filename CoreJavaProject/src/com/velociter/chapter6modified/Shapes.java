package com.velociter.chapter6modified;
interface Shapes {	    //making interface Shapes to write common methods inside it
double area();         //common method is declared in interface named area
double perimeter();     //common method is declared in interface named perimeter
}
